- [x] add search result count feedback, shows "X results found" or "No results found" or "Request failed" or "Searching..."
- [x] add "jump to target terminal" button to each search result, if the result uuid matches one of the current running terminal uuid, otherwise grayout the button. generate terminal uuid for each running terminal.
- [x] make session managing configurable: completely disable client side terminal capture, only use gnu screen or tmux as terminal session manager, so we can take image screenshots, extract text for each terminal session, without using frontend. configure if we use gnu screen or tmux in config file. also check if each binary is in path, if not in then prompt user to install and add to path. only if config file configured mode is check at the moment. make sure you use non-default server socket when launching gnu screen or tmux. if using tmux, unset TMUX environment variable, and create several environment variable settings to make sure nested tmux session works. related file: reference/terminal_screenshot_taking/tmux_kiosk.conf
- [x] update session db manager, store differently for tmux mode sessions and gnu screen mode sessions.
- [x] create example config yaml file in this directory named config.example.yaml
- [x] take screenshot png for each terminal, store as file png in backend
- [x] check out directory reference/flashback-screenshot-webui for reference, create or add webpage to get each historical terminal session detail, remember to take "terminal screenshot" or image in the backend, terminal text as "OCR" result, and store them in database, create timeline webpage, follow the style, make consistent look.
- [x] read files in reference/pty_session/{task.txt,attach_to_tmux_or_screen.sh,terminal.py,gnu_screen_capture_pane.md}, modify file flashback_terminal/terminal.py, use pty to attach to running tmux or screen session, complete task in reference/pty_session.task.txt
- [ ] set terminal current width and height to terminal manager (tmux/screen) backend class when frontend fires resize event.
- [ ] search does not work, maybe terminal capture for screen and tmux does not work.
- [ ] how should we order search results? by terminal id? terminal title? or by time? similarity? hybrid (time + similarity)? can we filter out those inactive sessions? can we specify time range in search?
- [ ] add attach option to "running session" in listing and searched candidates
- [ ] checkout how to set title in xterm.js for each terminal tab, and use xtitle to test it. (but seems konsole does it differently)
- [ ] improve subword search efficiency, like "james" in "jamesbrown"
    - by using bm25s? https://github.com/xhluca/bm25s
    - or just to use pystemmer after jieba tokenization? https://github.com/snowballstem/pystemmer 
- [ ] close all sessions spinned up by session_manager by using atexit.register, if config file or default config say so. make such config entry to determine whether to "stop" all sessions at exit.
- [ ] keyword highlight in search candidates
- [ ] make terminal workers threaded per backend attached tmux/screen process, and do not constantly poll them. instead, use event driven approach, like using tornado process protocol. make sure we will use no async await in code, including websocket. make sure each thread is separated.
- [ ] separate terminal server and webui, first spin up server from cli to receive terminal creation, reconnect request, do periodic tasks and run workers, then webui just attach to the server designated socket and method, perform search etc.
- [ ] search result is empty again. also try to refresh index in the background.
- [ ] terminal screenshot not working properly. pending test.
- [ ] disable full scrollback capture, use only recent lines
- [ ] why bm25index background process will not skip quickly indexed ones? skip those already in memory, do not read from database, just use in memory index. also mark those missing in database.
- [ ] increase logging in bm25 sqlite, make it async. why loading from db is damn slow? and refreshing index too? is it because of too large of the document length? log that too. log time used for each document. show warning for large document and long loading time, long loop and long function calls.
- [ ] add healthcheck api endpoint in backend, (already added in /healthcheck, will return {"status": "ok"}) test if terminal is offline in frontend, reattach if possible from frontend. (backend may also persist session, but leave it for simplicity for now? or use a button called "reattach all unattached, running sessions" in frontend?)